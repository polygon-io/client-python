---
kind: pipeline
type: docker
name: default

platform:
  os: linux
  arch: amd64

steps:
- name: test
  image: python:3.7.5-alpine
  environment:
    API_KEY:
      from_secret: TEST_API_KEY
    MY_NEW_ENV: "hello world"
  commands:
  - pip install -U tox
  - env
  - tox

trigger:
  branch:
  - master
  event:
  - pull_request

...
